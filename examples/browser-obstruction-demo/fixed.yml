# yaml-language-server: $schema=../../schema/art50-ci.schema.json
version: 1

project:
  name: Browser obstruction fixed fixture

browser:
  viewport:
    width: 640
    height: 480

output:
  directory: ../../.art50-ci/browser-obstruction-fixed
  screenshots: true

surfaces:
  - id: disclosure-obstruction-demo
    name: AI generation notice above the cookie overlay
    target: ./fixed.html
    disclosures:
      - id: ai-generation-notice
        selector: "[data-ai-disclosure]"
        expectedText: "generated by AI"
        accessibleName: "AI generation notice"
